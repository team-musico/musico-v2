export type Track = {
  id: string;
  videoId: string;
  title: string;
  artist: string;
  requestedTitle: string;
  coverUrl?: string;
  trackId?: number;
  thumbnail?: string;
  duration?: string;
  url: string;
  sourceTitle: string;
};
